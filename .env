# Flask application settings
FLASK_APP=run.py                    # Tells Flask which file to run
FLASK_ENV=development               # Sets the environment (development/production)
SECRET_KEY=your-secret-key-here     # Used for session security, change this!

# Database configuration
DATABASE_URL=sqlite:///app.db       # SQLite database path (for development)

# Email configuration (for user registration and password reset)
MAIL_SERVER=smtp.gmail.com          # SMTP server for sending emails
MAIL_PORT=587                       # SMTP port (587 for TLS)
MAIL_USE_TLS=True                   # Use TLS for secure email
MAIL_USERNAME=KRRusev21@codingburgas.bg  # Your email address
MAIL_PASSWORD=your-app-password     # Your email app password (not your regular password)